/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { couponsDeleteCoupon } from "../../funcs/couponsDeleteCoupon.js";
import { DeleteCouponRequest$zodSchema } from "../../models/deletecouponop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteCouponRequest$zodSchema,
};

export const tool$couponsDeleteCoupon: ToolDefinition<typeof args> = {
  name: "coupons-delete-coupon",
  description: `Delete coupon

Use when retiring a coupon (e.g. campaign ended). Returns 200 with success message.`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await couponsDeleteCoupon(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
