/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { creditNotesCreateCreditNote } from "../../funcs/creditNotesCreateCreditNote.js";
import { DtoCreateCreditNoteRequest$zodSchema } from "../../models/dtocreatecreditnoterequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DtoCreateCreditNoteRequest$zodSchema,
};

export const tool$creditNotesCreateCreditNote: ToolDefinition<typeof args> = {
  name: "credit-notes-create-credit-note",
  description: `Create credit note

Use when issuing a refund or adjustment (e.g. customer dispute or proration). Links to an invoice; create as draft then finalize.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await creditNotesCreateCreditNote(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
