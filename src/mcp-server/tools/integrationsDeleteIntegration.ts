/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsDeleteIntegration } from "../../funcs/integrationsDeleteIntegration.js";
import { DeleteIntegrationRequest$zodSchema } from "../../models/deleteintegrationop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteIntegrationRequest$zodSchema,
};

export const tool$integrationsDeleteIntegration: ToolDefinition<typeof args> = {
  name: "integrations-delete-integration",
  description: `Delete an integration

Use when disconnecting an integration (e.g. switching provider or removing OAuth). Deletes stored credentials.`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await integrationsDeleteIntegration(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
