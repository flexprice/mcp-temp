/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { entitlementsGetAddonEntitlements } from "../../funcs/entitlementsGetAddonEntitlements.js";
import { GetAddonEntitlementsRequest$zodSchema } from "../../models/getaddonentitlementsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetAddonEntitlementsRequest$zodSchema,
};

export const tool$entitlementsGetAddonEntitlements: ToolDefinition<
  typeof args
> = {
  name: "entitlements-get-addon-entitlements",
  description: `Get addon entitlements

Use when checking what features or limits an addon grants (e.g. for display or entitlement logic).`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await entitlementsGetAddonEntitlements(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
