/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { groupsDeleteGroup } from "../../funcs/groupsDeleteGroup.js";
import { DeleteGroupRequest$zodSchema } from "../../models/deletegroupop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteGroupRequest$zodSchema,
};

export const tool$groupsDeleteGroup: ToolDefinition<typeof args> = {
  name: "groups-delete-group",
  description: `Delete group

Use when removing a group and clearing its entity associations (e.g. retiring a product line). Returns 204 or 200 on success.`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await groupsDeleteGroup(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
