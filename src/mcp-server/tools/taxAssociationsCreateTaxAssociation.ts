/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { taxAssociationsCreateTaxAssociation } from "../../funcs/taxAssociationsCreateTaxAssociation.js";
import { DtoCreateTaxAssociationRequest$zodSchema } from "../../models/dtocreatetaxassociationrequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DtoCreateTaxAssociationRequest$zodSchema,
};

export const tool$taxAssociationsCreateTaxAssociation: ToolDefinition<
  typeof args
> = {
  name: "tax-associations-create-tax-association",
  description: `Create Tax Association

Use when linking a tax rate to an entity (e.g. customer, product, or region) so that rate applies on invoices.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await taxAssociationsCreateTaxAssociation(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
