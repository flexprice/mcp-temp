/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { taxAssociationsListTaxAssociations } from "../../funcs/taxAssociationsListTaxAssociations.js";
import { ListTaxAssociationsRequest$zodSchema } from "../../models/listtaxassociationsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListTaxAssociationsRequest$zodSchema.optional(),
};

export const tool$taxAssociationsListTaxAssociations: ToolDefinition<
  typeof args
> = {
  name: "tax-associations-list-tax-associations",
  description: `List tax associations

Use when listing tax associations (e.g. tax config or audit). Returns list with optional filtering.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await taxAssociationsListTaxAssociations(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
