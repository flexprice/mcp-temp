/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { secretsDeleteApiKey } from "../../funcs/secretsDeleteApiKey.js";
import { DeleteApiKeyRequest$zodSchema } from "../../models/deleteapikeyop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteApiKeyRequest$zodSchema,
};

export const tool$secretsDeleteApiKey: ToolDefinition<typeof args> = {
  name: "secrets-delete-api-key",
  description: `Delete an API key

Use when revoking an API key (e.g. rotation or compromise). Permanently invalidates the key.`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await secretsDeleteApiKey(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
