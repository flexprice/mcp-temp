/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { taxRatesGetTaxRate } from "../../funcs/taxRatesGetTaxRate.js";
import { GetTaxRateRequest$zodSchema } from "../../models/gettaxrateop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetTaxRateRequest$zodSchema,
};

export const tool$taxRatesGetTaxRate: ToolDefinition<typeof args> = {
  name: "tax-rates-get-tax-rate",
  description: `Get a tax rate

Use when you need to load a single tax rate (e.g. for display or when creating an association).`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await taxRatesGetTaxRate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
