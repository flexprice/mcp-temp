/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksHandleRazorpayWebhook } from "../../funcs/webhooksHandleRazorpayWebhook.js";
import { HandleRazorpayWebhookRequest$zodSchema } from "../../models/handlerazorpaywebhookop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: HandleRazorpayWebhookRequest$zodSchema,
};

export const tool$webhooksHandleRazorpayWebhook: ToolDefinition<typeof args> = {
  name: "webhooks-handle-razorpay-webhook",
  description: `Handle Razorpay webhook events

Use as the Razorpay webhook endpoint URL. Receives payment capture and failure events to update invoice or payment status in FlexPrice.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await webhooksHandleRazorpayWebhook(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
