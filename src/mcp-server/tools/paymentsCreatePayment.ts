/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { paymentsCreatePayment } from "../../funcs/paymentsCreatePayment.js";
import { DtoCreatePaymentRequest$zodSchema } from "../../models/dtocreatepaymentrequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DtoCreatePaymentRequest$zodSchema,
};

export const tool$paymentsCreatePayment: ToolDefinition<typeof args> = {
  name: "payments-create-payment",
  description: `Create payment

Use when recording a payment against an invoice (e.g. after receiving funds via a gateway or manual entry).`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await paymentsCreatePayment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
