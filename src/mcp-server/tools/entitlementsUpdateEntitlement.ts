/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { entitlementsUpdateEntitlement } from "../../funcs/entitlementsUpdateEntitlement.js";
import { UpdateEntitlementRequest$zodSchema } from "../../models/updateentitlementop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateEntitlementRequest$zodSchema,
};

export const tool$entitlementsUpdateEntitlement: ToolDefinition<typeof args> = {
  name: "entitlements-update-entitlement",
  description: `Update entitlement

Use when changing an entitlement (e.g. increasing or decreasing a limit). Request body contains the fields to update.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await entitlementsUpdateEntitlement(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
