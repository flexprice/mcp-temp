/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { priceUnitsQueryPriceUnit } from "../../funcs/priceUnitsQueryPriceUnit.js";
import { TypesPriceUnitFilter$zodSchema } from "../../models/typespriceunitfilter.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: TypesPriceUnitFilter$zodSchema,
};

export const tool$priceUnitsQueryPriceUnit: ToolDefinition<typeof args> = {
  name: "price-units-query-price-unit",
  description: `Query price units

Use when searching or listing price units (e.g. admin catalog). Returns a paginated list; supports filtering and sorting.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await priceUnitsQueryPriceUnit(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
