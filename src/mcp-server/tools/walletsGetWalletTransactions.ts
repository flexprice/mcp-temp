/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { walletsGetWalletTransactions } from "../../funcs/walletsGetWalletTransactions.js";
import { GetWalletTransactionsRequest$zodSchema } from "../../models/getwallettransactionsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetWalletTransactionsRequest$zodSchema,
};

export const tool$walletsGetWalletTransactions: ToolDefinition<typeof args> = {
  name: "wallets-get-wallet-transactions",
  description: `Get wallet transactions

Use when showing transaction history for a wallet (e.g. credit/debit log or audit). Returns a paginated list; supports limit, offset, and filters.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await walletsGetWalletTransactions(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
