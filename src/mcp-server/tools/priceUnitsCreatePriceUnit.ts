/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { priceUnitsCreatePriceUnit } from "../../funcs/priceUnitsCreatePriceUnit.js";
import { DtoCreatePriceUnitRequest$zodSchema } from "../../models/dtocreatepriceunitrequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DtoCreatePriceUnitRequest$zodSchema,
};

export const tool$priceUnitsCreatePriceUnit: ToolDefinition<typeof args> = {
  name: "price-units-create-price-unit",
  description: `Create price unit

Use when defining a new unit of measure for pricing (e.g. GB, API call, seat). Ideal for metered or usage-based prices.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await priceUnitsCreatePriceUnit(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
