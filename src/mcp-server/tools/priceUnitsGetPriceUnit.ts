/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { priceUnitsGetPriceUnit } from "../../funcs/priceUnitsGetPriceUnit.js";
import { GetPriceUnitRequest$zodSchema } from "../../models/getpriceunitop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetPriceUnitRequest$zodSchema,
};

export const tool$priceUnitsGetPriceUnit: ToolDefinition<typeof args> = {
  name: "price-units-get-price-unit",
  description: `Get price unit

Use when you need to load a single price unit (e.g. for display or when creating a price).`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await priceUnitsGetPriceUnit(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
