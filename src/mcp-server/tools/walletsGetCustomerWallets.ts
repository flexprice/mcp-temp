/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { walletsGetCustomerWallets } from "../../funcs/walletsGetCustomerWallets.js";
import { GetCustomerWalletsRequest$zodSchema } from "../../models/getcustomerwalletsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetCustomerWalletsRequest$zodSchema.optional(),
};

export const tool$walletsGetCustomerWallets: ToolDefinition<typeof args> = {
  name: "wallets-get-customer-wallets",
  description: `Get Customer Wallets

Use when resolving wallets by external customer id or lookup key (e.g. from your app's user id). Supports optional real-time balance and expand.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await walletsGetCustomerWallets(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
