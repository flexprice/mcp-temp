/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { scheduledTasksDeleteScheduledTask } from "../../funcs/scheduledTasksDeleteScheduledTask.js";
import { DeleteScheduledTaskRequest$zodSchema } from "../../models/deletescheduledtaskop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteScheduledTaskRequest$zodSchema,
};

export const tool$scheduledTasksDeleteScheduledTask: ToolDefinition<
  typeof args
> = {
  name: "scheduled-tasks-delete-scheduled-task",
  description: `Delete a scheduled task

Use when removing a scheduled task from the active roster. Archives the task and removes it from the scheduler (soft delete).`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await scheduledTasksDeleteScheduledTask(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
