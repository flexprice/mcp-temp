/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { creditNotesGetCreditNote } from "../../funcs/creditNotesGetCreditNote.js";
import { GetCreditNoteRequest$zodSchema } from "../../models/getcreditnoteop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetCreditNoteRequest$zodSchema,
};

export const tool$creditNotesGetCreditNote: ToolDefinition<typeof args> = {
  name: "credit-notes-get-credit-note",
  description: `Get credit note

Use when you need to load a single credit note (e.g. for display or reconciliation).`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await creditNotesGetCreditNote(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
