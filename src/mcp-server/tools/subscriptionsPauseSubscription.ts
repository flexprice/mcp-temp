/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { subscriptionsPauseSubscription } from "../../funcs/subscriptionsPauseSubscription.js";
import { PauseSubscriptionRequest$zodSchema } from "../../models/pausesubscriptionop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PauseSubscriptionRequest$zodSchema,
};

export const tool$subscriptionsPauseSubscription: ToolDefinition<typeof args> =
  {
    name: "subscriptions-pause-subscription",
    description: `Pause a subscription

Use when temporarily stopping a subscription (e.g. customer hold or seasonal pause). Billing and access pause; resume when ready.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await subscriptionsPauseSubscription(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
