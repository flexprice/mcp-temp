/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { costsQueryCostsheet } from "../../funcs/costsQueryCostsheet.js";
import { CostsheetFilter$zodSchema } from "../../models/costsheetfilter.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CostsheetFilter$zodSchema,
};

export const tool$costsQueryCostsheet: ToolDefinition<typeof args> = {
  name: "costs-query-costsheet",
  description: `Query costsheets

Use when listing or searching costsheets (e.g. admin catalog). Returns a paginated list; supports filtering and sorting.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await costsQueryCostsheet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
