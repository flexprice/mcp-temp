/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { priceUnitsListPriceUnits } from "../../funcs/priceUnitsListPriceUnits.js";
import { ListPriceUnitsRequest$zodSchema } from "../../models/listpriceunitsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListPriceUnitsRequest$zodSchema.optional(),
};

export const tool$priceUnitsListPriceUnits: ToolDefinition<typeof args> = {
  name: "price-units-list-price-units",
  description: `List price units

Use when listing price units (e.g. in a catalog or when creating prices). Returns a paginated list; supports status, sort, and pagination.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await priceUnitsListPriceUnits(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
