/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { secretsListApiKeys } from "../../funcs/secretsListApiKeys.js";
import { ListApiKeysRequest$zodSchema } from "../../models/listapikeysop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListApiKeysRequest$zodSchema.optional(),
};

export const tool$secretsListApiKeys: ToolDefinition<typeof args> = {
  name: "secrets-list-api-keys",
  description: `List API keys

Use when listing API keys (e.g. admin view or rotating keys). Returns a paginated list.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await secretsListApiKeys(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
