/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { costsCreateCostsheet } from "../../funcs/costsCreateCostsheet.js";
import { DtoCreateCostsheetRequest$zodSchema } from "../../models/dtocreatecostsheetrequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DtoCreateCostsheetRequest$zodSchema,
};

export const tool$costsCreateCostsheet: ToolDefinition<typeof args> = {
  name: "costs-create-costsheet",
  description: `Create costsheet

Use when setting up a new pricing configuration (e.g. a new product or region). Costsheets group prices and define the default for the environment.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await costsCreateCostsheet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
