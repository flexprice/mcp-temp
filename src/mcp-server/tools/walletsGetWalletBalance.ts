/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { walletsGetWalletBalance } from "../../funcs/walletsGetWalletBalance.js";
import { GetWalletBalanceRequest$zodSchema } from "../../models/getwalletbalanceop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetWalletBalanceRequest$zodSchema,
};

export const tool$walletsGetWalletBalance: ToolDefinition<typeof args> = {
  name: "wallets-get-wallet-balance",
  description: `Get wallet balance

Use when displaying or checking current wallet balance (e.g. before charging or in a portal). Supports optional expand for credits breakdown and from_cache.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await walletsGetWalletBalance(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
