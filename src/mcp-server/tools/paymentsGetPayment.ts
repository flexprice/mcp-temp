/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { paymentsGetPayment } from "../../funcs/paymentsGetPayment.js";
import { GetPaymentRequest$zodSchema } from "../../models/getpaymentop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetPaymentRequest$zodSchema,
};

export const tool$paymentsGetPayment: ToolDefinition<typeof args> = {
  name: "payments-get-payment",
  description: `Get payment

Use when you need to load a single payment (e.g. for a receipt view or reconciliation).`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await paymentsGetPayment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
