/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tenantsUpdateTenant } from "../../funcs/tenantsUpdateTenant.js";
import { DtoUpdateTenantRequest$zodSchema } from "../../models/dtoupdatetenantrequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DtoUpdateTenantRequest$zodSchema,
};

export const tool$tenantsUpdateTenant: ToolDefinition<typeof args> = {
  name: "tenants-update-tenant",
  description: `Update a tenant

Use when changing tenant details (e.g. name or billing info). Request body contains the fields to update.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await tenantsUpdateTenant(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
