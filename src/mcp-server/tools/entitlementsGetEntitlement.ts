/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { entitlementsGetEntitlement } from "../../funcs/entitlementsGetEntitlement.js";
import { GetEntitlementRequest$zodSchema } from "../../models/getentitlementop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetEntitlementRequest$zodSchema,
};

export const tool$entitlementsGetEntitlement: ToolDefinition<typeof args> = {
  name: "entitlements-get-entitlement",
  description: `Get entitlement

Use when you need to load a single entitlement (e.g. to display or edit a feature limit).`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await entitlementsGetEntitlement(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
