/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { priceUnitsUpdatePriceUnit } from "../../funcs/priceUnitsUpdatePriceUnit.js";
import { UpdatePriceUnitRequest$zodSchema } from "../../models/updatepriceunitop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdatePriceUnitRequest$zodSchema,
};

export const tool$priceUnitsUpdatePriceUnit: ToolDefinition<typeof args> = {
  name: "price-units-update-price-unit",
  description: `Update price unit

Use when renaming or updating metadata for a price unit. Code is immutable once created.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await priceUnitsUpdatePriceUnit(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
