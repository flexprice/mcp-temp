/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { priceUnitsGetPriceUnitByCode } from "../../funcs/priceUnitsGetPriceUnitByCode.js";
import { GetPriceUnitByCodeRequest$zodSchema } from "../../models/getpriceunitbycodeop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetPriceUnitByCodeRequest$zodSchema,
};

export const tool$priceUnitsGetPriceUnitByCode: ToolDefinition<typeof args> = {
  name: "price-units-get-price-unit-by-code",
  description: `Get price unit by code

Use when resolving a price unit by code (e.g. from an external catalog or config). Ideal for integrations.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await priceUnitsGetPriceUnitByCode(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
