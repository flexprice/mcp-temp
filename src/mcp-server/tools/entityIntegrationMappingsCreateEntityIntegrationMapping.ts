/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { entityIntegrationMappingsCreateEntityIntegrationMapping } from "../../funcs/entityIntegrationMappingsCreateEntityIntegrationMapping.js";
import { DtoCreateEntityIntegrationMappingRequest$zodSchema } from "../../models/dtocreateentityintegrationmappingrequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DtoCreateEntityIntegrationMappingRequest$zodSchema,
};

export const tool$entityIntegrationMappingsCreateEntityIntegrationMapping:
  ToolDefinition<typeof args> = {
    name: "entity-integration-mappings-create-entity-8da",
    description: `Create entity integration mapping

Use when linking a FlexPrice entity to an external system (e.g. CRM or payment provider) so you can sync or reconcile by external ID.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] =
        await entityIntegrationMappingsCreateEntityIntegrationMapping(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
