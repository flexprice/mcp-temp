/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { priceUnitsDeletePriceUnit } from "../../funcs/priceUnitsDeletePriceUnit.js";
import { DeletePriceUnitRequest$zodSchema } from "../../models/deletepriceunitop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeletePriceUnitRequest$zodSchema,
};

export const tool$priceUnitsDeletePriceUnit: ToolDefinition<typeof args> = {
  name: "price-units-delete-price-unit",
  description: `Delete price unit

Use when removing a price unit that is no longer needed. Fails if any price references this unit.`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await priceUnitsDeletePriceUnit(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
