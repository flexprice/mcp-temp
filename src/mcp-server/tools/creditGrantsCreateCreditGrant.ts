/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { creditGrantsCreateCreditGrant } from "../../funcs/creditGrantsCreateCreditGrant.js";
import { DtoCreateCreditGrantRequest$zodSchema } from "../../models/dtocreatecreditgrantrequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DtoCreateCreditGrantRequest$zodSchema,
};

export const tool$creditGrantsCreateCreditGrant: ToolDefinition<typeof args> = {
  name: "credit-grants-create-credit-grant",
  description: `Create credit grant

Use when giving a customer or plan credits (e.g. prepaid balance or promotional credits). Scope can be plan or subscription; supports start/end dates.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await creditGrantsCreateCreditGrant(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
