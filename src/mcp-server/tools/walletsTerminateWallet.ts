/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { walletsTerminateWallet } from "../../funcs/walletsTerminateWallet.js";
import { TerminateWalletRequest$zodSchema } from "../../models/terminatewalletop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: TerminateWalletRequest$zodSchema,
};

export const tool$walletsTerminateWallet: ToolDefinition<typeof args> = {
  name: "wallets-terminate-wallet",
  description: `Terminate a wallet

Use when closing a customer wallet (e.g. churn or migration). Closes the wallet and applies remaining balance per policy (refund or forfeit).`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await walletsTerminateWallet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
