/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksHandleHubspotWebhook } from "../../funcs/webhooksHandleHubspotWebhook.js";
import { HandleHubspotWebhookRequest$zodSchema } from "../../models/handlehubspotwebhookop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: HandleHubspotWebhookRequest$zodSchema,
};

export const tool$webhooksHandleHubspotWebhook: ToolDefinition<typeof args> = {
  name: "webhooks-handle-hubspot-webhook",
  description: `Handle HubSpot webhook events

Use as the HubSpot webhook endpoint URL. Receives deal and customer events (e.g. deal closed won) to create or update customers in FlexPrice.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await webhooksHandleHubspotWebhook(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
