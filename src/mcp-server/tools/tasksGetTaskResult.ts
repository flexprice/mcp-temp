/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tasksGetTaskResult } from "../../funcs/tasksGetTaskResult.js";
import { GetTaskResultRequest$zodSchema } from "../../models/gettaskresultop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetTaskResultRequest$zodSchema,
};

export const tool$tasksGetTaskResult: ToolDefinition<typeof args> = {
  name: "tasks-get-task-result",
  description: `Get task processing result

Use when fetching the outcome of a completed task (e.g. export URL or error message). Call after task status is complete.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await tasksGetTaskResult(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
