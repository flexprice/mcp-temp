/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersQueryUser } from "../../funcs/usersQueryUser.js";
import { TypesUserFilter$zodSchema } from "../../models/typesuserfilter.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: TypesUserFilter$zodSchema,
};

export const tool$usersQueryUser: ToolDefinition<typeof args> = {
  name: "users-query-user",
  description: `Query users

Use when listing or searching service accounts in an admin UI, or when auditing who has API access and which roles they have.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await usersQueryUser(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
