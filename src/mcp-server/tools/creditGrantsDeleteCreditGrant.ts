/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { creditGrantsDeleteCreditGrant } from "../../funcs/creditGrantsDeleteCreditGrant.js";
import { DeleteCreditGrantRequest$zodSchema } from "../../models/deletecreditgrantop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteCreditGrantRequest$zodSchema,
};

export const tool$creditGrantsDeleteCreditGrant: ToolDefinition<typeof args> = {
  name: "credit-grants-delete-credit-grant",
  description: `Delete credit grant

Use when removing or ending a credit grant (e.g. revoke promo or close prepaid). Plan-scoped grants are archived; subscription-scoped supports optional effective_date in body.`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await creditGrantsDeleteCreditGrant(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
