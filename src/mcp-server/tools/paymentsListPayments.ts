/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { paymentsListPayments } from "../../funcs/paymentsListPayments.js";
import { ListPaymentsRequest$zodSchema } from "../../models/listpaymentsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListPaymentsRequest$zodSchema.optional(),
};

export const tool$paymentsListPayments: ToolDefinition<typeof args> = {
  name: "payments-list-payments",
  description: `List payments

Use when listing or searching payments (e.g. reconciliation UI or customer payment history). Returns a paginated list; supports filtering by customer, invoice, status.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await paymentsListPayments(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
