/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { scheduledTasksCreateScheduledTask } from "../../funcs/scheduledTasksCreateScheduledTask.js";
import { DtoCreateScheduledTaskRequest$zodSchema } from "../../models/dtocreatescheduledtaskrequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DtoCreateScheduledTaskRequest$zodSchema,
};

export const tool$scheduledTasksCreateScheduledTask: ToolDefinition<
  typeof args
> = {
  name: "scheduled-tasks-create-scheduled-task",
  description: `Create scheduled task

Use when setting up recurring data exports or other scheduled jobs. Ideal for report generation or syncing data on a schedule.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await scheduledTasksCreateScheduledTask(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
