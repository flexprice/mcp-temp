/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { walletsGetWalletsByCustomerId } from "../../funcs/walletsGetWalletsByCustomerId.js";
import { GetWalletsByCustomerIdRequest$zodSchema } from "../../models/getwalletsbycustomeridop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetWalletsByCustomerIdRequest$zodSchema,
};

export const tool$walletsGetWalletsByCustomerId: ToolDefinition<typeof args> = {
  name: "wallets-get-wallets-by-customer-id",
  description: `Get wallets by customer ID

Use when showing a customer's wallets (e.g. balance overview by currency or in a billing portal). Supports optional expand for balance breakdown.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await walletsGetWalletsByCustomerId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
