/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tasksCreateTask } from "../../funcs/tasksCreateTask.js";
import { DtoCreateTaskRequest$zodSchema } from "../../models/dtocreatetaskrequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DtoCreateTaskRequest$zodSchema,
};

export const tool$tasksCreateTask: ToolDefinition<typeof args> = {
  name: "tasks-create-task",
  description: `Create a new task

Use when submitting a file or job for async processing (e.g. export or import). Returns task ID to poll for status and result.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await tasksCreateTask(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
