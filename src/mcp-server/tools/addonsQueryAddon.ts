/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { addonsQueryAddon } from "../../funcs/addonsQueryAddon.js";
import { TypesAddonFilter$zodSchema } from "../../models/typesaddonfilter.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: TypesAddonFilter$zodSchema,
};

export const tool$addonsQueryAddon: ToolDefinition<typeof args> = {
  name: "addons-query-addon",
  description: `Query addons

Use when listing or searching addons (e.g. catalog or subscription builder). Returns a paginated list; supports filtering and sorting.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await addonsQueryAddon(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
