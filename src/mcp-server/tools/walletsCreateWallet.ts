/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { walletsCreateWallet } from "../../funcs/walletsCreateWallet.js";
import { DtoCreateWalletRequest$zodSchema } from "../../models/dtocreatewalletrequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DtoCreateWalletRequest$zodSchema,
};

export const tool$walletsCreateWallet: ToolDefinition<typeof args> = {
  name: "wallets-create-wallet",
  description: `Create a new wallet

Use when giving a customer a prepaid or credit balance (e.g. prepaid plans or promotional credits).`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await walletsCreateWallet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
