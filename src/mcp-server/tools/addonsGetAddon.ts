/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { addonsGetAddon } from "../../funcs/addonsGetAddon.js";
import { GetAddonRequest$zodSchema } from "../../models/getaddonop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetAddonRequest$zodSchema,
};

export const tool$addonsGetAddon: ToolDefinition<typeof args> = {
  name: "addons-get-addon",
  description: `Get addon

Use when you need to load a single addon (e.g. for display or to attach to a subscription).`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await addonsGetAddon(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
