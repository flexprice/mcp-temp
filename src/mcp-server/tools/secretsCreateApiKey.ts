/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { secretsCreateApiKey } from "../../funcs/secretsCreateApiKey.js";
import { DtoCreateAPIKeyRequest$zodSchema } from "../../models/dtocreateapikeyrequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DtoCreateAPIKeyRequest$zodSchema,
};

export const tool$secretsCreateApiKey: ToolDefinition<typeof args> = {
  name: "secrets-create-api-key",
  description: `Create a new API key

Use when issuing a new API key (e.g. for a service account or for the current user). Provide service_account_id to create for a service account.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await secretsCreateApiKey(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
