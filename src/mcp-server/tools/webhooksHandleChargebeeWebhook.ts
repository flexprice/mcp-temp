/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksHandleChargebeeWebhook } from "../../funcs/webhooksHandleChargebeeWebhook.js";
import { HandleChargebeeWebhookRequest$zodSchema } from "../../models/handlechargebeewebhookop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: HandleChargebeeWebhookRequest$zodSchema,
};

export const tool$webhooksHandleChargebeeWebhook: ToolDefinition<typeof args> =
  {
    name: "webhooks-handle-chargebee-webhook",
    description: `Handle Chargebee webhook events

Use as the Chargebee webhook endpoint URL. Receives payment and subscription events from Chargebee to sync status into FlexPrice.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await webhooksHandleChargebeeWebhook(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
