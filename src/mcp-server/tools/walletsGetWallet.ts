/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { walletsGetWallet } from "../../funcs/walletsGetWallet.js";
import { GetWalletRequest$zodSchema } from "../../models/getwalletop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetWalletRequest$zodSchema,
};

export const tool$walletsGetWallet: ToolDefinition<typeof args> = {
  name: "wallets-get-wallet",
  description: `Get wallet

Use when you need to load a single wallet (e.g. for a balance or settings view).`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await walletsGetWallet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
