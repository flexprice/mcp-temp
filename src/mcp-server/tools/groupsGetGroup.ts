/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { groupsGetGroup } from "../../funcs/groupsGetGroup.js";
import { GetGroupRequest$zodSchema } from "../../models/getgroupop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetGroupRequest$zodSchema,
};

export const tool$groupsGetGroup: ToolDefinition<typeof args> = {
  name: "groups-get-group",
  description: `Get group

Use when you need to load a single group (e.g. for display or to assign entities).`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await groupsGetGroup(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
