/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tasksListTasks } from "../../funcs/tasksListTasks.js";
import { ListTasksRequest$zodSchema } from "../../models/listtasksop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ListTasksRequest$zodSchema.optional(),
};

export const tool$tasksListTasks: ToolDefinition<typeof args> = {
  name: "tasks-list-tasks",
  description: `List tasks

Use when listing or searching async tasks (e.g. admin queue view). Returns list with optional filtering.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await tasksListTasks(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
