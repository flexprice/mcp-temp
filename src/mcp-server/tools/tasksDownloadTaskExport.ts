/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tasksDownloadTaskExport } from "../../funcs/tasksDownloadTaskExport.js";
import { DownloadTaskExportRequest$zodSchema } from "../../models/downloadtaskexportop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DownloadTaskExportRequest$zodSchema,
};

export const tool$tasksDownloadTaskExport: ToolDefinition<typeof args> = {
  name: "tasks-download-task-export",
  description: `Download task export file

Use when letting a user download an exported file (e.g. report or data export). Returns a presigned URL; supports FlexPrice or customer-owned S3.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await tasksDownloadTaskExport(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
