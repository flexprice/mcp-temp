/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { creditGrantsUpdateCreditGrant } from "../../funcs/creditGrantsUpdateCreditGrant.js";
import { UpdateCreditGrantRequest$zodSchema } from "../../models/updatecreditgrantop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateCreditGrantRequest$zodSchema,
};

export const tool$creditGrantsUpdateCreditGrant: ToolDefinition<typeof args> = {
  name: "credit-grants-update-credit-grant",
  description: `Update credit grant

Use when changing a credit grant (e.g. amount or end date). Request body contains the fields to update.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await creditGrantsUpdateCreditGrant(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
