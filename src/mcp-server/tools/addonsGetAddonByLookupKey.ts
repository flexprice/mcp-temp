/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { addonsGetAddonByLookupKey } from "../../funcs/addonsGetAddonByLookupKey.js";
import { GetAddonByLookupKeyRequest$zodSchema } from "../../models/getaddonbylookupkeyop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetAddonByLookupKeyRequest$zodSchema,
};

export const tool$addonsGetAddonByLookupKey: ToolDefinition<typeof args> = {
  name: "addons-get-addon-by-lookup-key",
  description: `Get addon by lookup key

Use when resolving an addon by external id (e.g. from your product catalog). Ideal for integrations.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await addonsGetAddonByLookupKey(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
