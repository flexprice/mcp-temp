/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { walletsQueryWallet } from "../../funcs/walletsQueryWallet.js";
import { TypesWalletFilter$zodSchema } from "../../models/typeswalletfilter.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: TypesWalletFilter$zodSchema,
};

export const tool$walletsQueryWallet: ToolDefinition<typeof args> = {
  name: "wallets-query-wallet",
  description: `Query wallets

Use when listing or searching wallets (e.g. admin view or reporting). Returns a paginated list; supports filtering by customer and status.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await walletsQueryWallet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
