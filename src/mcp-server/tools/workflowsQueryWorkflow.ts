/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { workflowsQueryWorkflow } from "../../funcs/workflowsQueryWorkflow.js";
import { TypesWorkflowExecutionFilter$zodSchema } from "../../models/typesworkflowexecutionfilter.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: TypesWorkflowExecutionFilter$zodSchema,
};

export const tool$workflowsQueryWorkflow: ToolDefinition<typeof args> = {
  name: "workflows-query-workflow",
  description: `Query workflows

Use when listing or auditing workflow runs (e.g. ops dashboard or debugging). Returns a paginated list; supports filtering by workflow type and status.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await workflowsQueryWorkflow(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
