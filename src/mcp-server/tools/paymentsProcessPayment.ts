/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { paymentsProcessPayment } from "../../funcs/paymentsProcessPayment.js";
import { ProcessPaymentRequest$zodSchema } from "../../models/processpaymentop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ProcessPaymentRequest$zodSchema,
};

export const tool$paymentsProcessPayment: ToolDefinition<typeof args> = {
  name: "payments-process-payment",
  description: `Process payment

Use when you need to charge or process a payment (e.g. trigger the payment provider to capture funds). Returns updated payment with status.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await paymentsProcessPayment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
