/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { alertsQueryAlertLog } from "../../funcs/alertsQueryAlertLog.js";
import { TypesAlertLogFilter$zodSchema } from "../../models/typesalertlogfilter.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: TypesAlertLogFilter$zodSchema,
};

export const tool$alertsQueryAlertLog: ToolDefinition<typeof args> = {
  name: "alerts-query-alert-log",
  description: `Query alert logs

Use when viewing or searching alert history (e.g. support triage or customer-facing alert log). Returns a paginated list; supports filtering by type, customer, subscription.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await alertsQueryAlertLog(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
