/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { costsUpdateCostsheet } from "../../funcs/costsUpdateCostsheet.js";
import { UpdateCostsheetRequest$zodSchema } from "../../models/updatecostsheetop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: UpdateCostsheetRequest$zodSchema,
};

export const tool$costsUpdateCostsheet: ToolDefinition<typeof args> = {
  name: "costs-update-costsheet",
  description: `Update costsheet

Use when changing costsheet name or metadata.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await costsUpdateCostsheet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
