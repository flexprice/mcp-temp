/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tenantsGetTenantById } from "../../funcs/tenantsGetTenantById.js";
import { GetTenantByIdRequest$zodSchema } from "../../models/gettenantbyidop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetTenantByIdRequest$zodSchema,
};

export const tool$tenantsGetTenantById: ToolDefinition<typeof args> = {
  name: "tenants-get-tenant-by-id",
  description: `Get tenant by ID

Get tenant by ID`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await tenantsGetTenantById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
